# SMOOSH! v6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Ship SMOOSH! v6.0.0 — slower crit/splash curves + steeper monster scaling, persistent drops, full-heal pets each stage, easier taps, sword attack icon, melee/skill JUICE + a frog-tongue PULL skill, whole-screen fever + fever BGM, +40 monster species, a finer nest grid with ~60 everyday decor items pets actually use, a cuter menu with the player's pets frolicking, a flashier gacha reveal, and premium rarity cards.

**Architecture:** Balance changes are pure `balance.js`/`config.js` + the 200-stage sim gate. Behavior fixes are surgical `game.js`/`pets.js` edits. Content (+40 monsters, +36 decor) is data appended to `catalog.js`/`decor.js` (parametric painters, no rewrite). Juice/art is FX in `effects.js`/scene files + `frames.js`. All test-gated where pure logic exists.

## Global Constraints
- Repo `D:\source\ai-game-studio\games-for-release\smoosh-mobile\`, branch `v6` off main; commit there.
- The 200-stage balance-invariant sim (`tests/balance.test.js`) is the load-bearing gate for A1/A2/A3 — retune until green, document final constants; never delete the invariant.
- Suite green at every commit (baseline 199); `node --check` every touched JS.
- All user-visible strings via I18n (locale forced 'en' since v5). English code/comments.
- Keep the v5 retro-neon look (CONFIG.PASTEL neon tokens, pixel font, CRT, pixel sprites); new art uses the same tokens + parametric painters; textures bake through BootScene (CONFIG.PIXEL, once-only, NEAREST).
- Phaser-guard scoping for any new scene class; dual-mode exports for modules.
- fitToWidth clamp on any new dynamic label (pixel font is 1.0em/char).

---

### Task 1: Crit + splash curves (A1, A2)
**Files:** `balance.js` (critChance/effective/splashRadius), `config.js` (splash maxLevel, FEVER.splashRadius), `tests/balance.test.js`, `tests/economy.test.js` if it pins these.
- `critChance(level) = Math.min(0.02 + 0.007*level, 0.22)`; in `effective()` clamp crit ≤0.5; clamp tap-time crit (buff auras) ≤0.6 in game.js applyTap.
- `splashRadius(level) = 14*level`; splash upgrade `maxLevel` 10→32 (max ≈448 ≈2× old 220); `CONFIG.FEVER.splashRadius` ×~2.
- Tests: new crit/splash values; re-run 200-stage sim, retune tap costGrowth/gold only if the taps band breaks (document). 
- [ ] TDD value asserts → implement → sim green; commit `feat(v6): slower crit + splash curves, ~2x splash ceiling`.

### Task 2: Steeper monster attack + HP scaling (A3)
**Files:** `balance.js` (mobHP, bossHpMult, new monsterAtkMult), `game.js` + `monsters.js` (apply monsterAtkMult to every monster→pet/nest damage), `tests/balance.test.js`.
- `mobHP(stage) = Math.round(3 * Math.pow(1.30, stage))`; steepen `bossHpMult`.
- `Balance.monsterAtkMult(stage) = Math.min(6, 1 + 0.03*stage)`; multiply every monster strike/DoT/nest-shell damage by it (find all monster→target damage sites: monsterStrikeArea, charge, DoT, nest shelling).
- Retune the sim: steeper HP raises taps → adjust economy (gold base / tap dmg growth / taps band) until the invariant passes; document final constants + the new expected difficulty.
- [ ] TDD → implement → sim green; commit `feat(v6): steeper per-stage monster HP + attack scaling (sim retuned)`.

### Task 3: Persistent drops + full-heal pets each stage (A4, A5)
**Files:** `game.js` (onStageClear: remove clearDrops; remove stageWouldClear roll-skip), `pets.js` (onStageStart: heal-all/revive-all/clear-status).
- Remove `this.clearDrops()` from `onStageClear` (keep in SHUTDOWN + onNestBroken). Remove the `stageWouldClear` branch so the final kill rolls a drop normally.
- `FieldPets.onStageStart()`: for every agent set hp=maxHp, clear status, cancel KO + revive if ko, reset flags. (Or call rebuild() — but heal-in-place preserves temp-agent purge; pick heal-all + keep the existing temp purge.)
- Tests: pure helper if extracted (drop persists predicate; a pet-heal-all pure fn) — or a behavior note; keep suite green.
- [ ] Implement → `node --check` → serve-note; commit `feat(v6): drops persist to lifetime + all pets full-heal each stage`.

### Task 4: Tap-friendly buttons + sword attack icon (A6, A7)
**Files:** `ui.js` (makeUiButton padded hit area; menu nav / shop tab / ult padding), `main.js` (procedural `up-tap` icon → sword).
- `makeUiButton`: `body.setInteractive(new Phaser.Geom.Rectangle(-14, -14, w+28, h+28) /* local */, Phaser.Geom.Rectangle.Contains)` (verify local vs origin coords for NineSlice; use the correct offset so the padded rect is centered on the button). Confirm hand cursor still works.
- Redraw `up-tap` in the icon painter as a sword (blade + guard + hilt), pixel-clean, using accent/ink tokens.
- [ ] Implement → `node --check` → commit `feat(v6): padded button hit-areas + sword attack-power icon`.

### Task 5: Melee attack juice (B1)
**Files:** `monsters.js` (melee strike callback) + `effects.js` (a `biteFx`).
- On melee strike: monster pinch-scale, `Effects.biteFx(scene,x,y,color)` (radial impact flash + 2-3 sparks), `Sfx` crunch. Additive only.
- [ ] Implement → commit `feat(v6): melee attacks have a visible bite impact`.

### Task 6: Skill visibility + PULL archetype + signature reassignment (B2)
**Files:** `skills.js` (new `pull` archetype + cast), `effects.js` (applySkillEffect: cast telegraph + per-archetype bigger FX + skill-name popup + pull handler), `monsters.js`/`pets.js` (telegraph + name popup on cast; shorten cd ×0.75; skip cd on empty-target whiff), `catalog.js` (reassign some monster/pet skills incl. `pull`), `tests/skills.test.js` + `tests/catalog.test.js`.
- `pull`: `Skills.cast('pull', ctx)` returns `{kind:'pull', targets:[nearestId], distance, amount}`; `Effects` handler tweens the target toward the caster + a damage tick + a tongue/line VFX.
- Every cast: brief caster wind-up flash (archetype color), a skill-name popup (`I18n`/uppercase), distinct FX per kind (bigger than the hit-ring). Cd ×0.75 in ARCHETYPES or at call site; whiff (no targets) does NOT set skillCdUntil.
- Reassign skills so each species reads distinct; assign `pull` to `frog` pet + ≥1 new monster.
- Tests: pull archetype exists + casts (drags nearest); distinctness/coverage still pass; whiff-no-cd invariant.
- [ ] TDD → implement → green; commit `feat(v6): dramatic skill FX + frog-tongue PULL + signature skill pass`.

### Task 7: Fever spectacle + fever BGM (B3)
**Files:** `game.js` (triggerFever/fever update: full-screen overlay + field tint + border for the duration), `effects.js` (fever overlay helpers), `sfx.js` (synth chiptune fever loop: `feverMusicStart`/`feverMusicStop`), `game.js` Feel.feverStart/End wire the loop.
- Fever duration: animated neon gradient/scanline pulse overlay, rainbow field tint, glowing border, denser particles; cleaned up on fever end.
- `sfx.js`: an upbeat looped arpeggio+bass synth, started on feverStart, stopped on feverEnd, silent when muted; no assets.
- Tests: a pure guard test that the fever-music start no-ops when muted (if the mute check is pure-testable); else node --check + note.
- [ ] Implement → commit `feat(v6): whole-screen fever spectacle + upbeat fever BGM loop`.

### Task 8: +40 monster species (C1)
**Files:** `catalog.js` (append 40 `species(...)` entries), `spawner.js` (add new heavy/tricky ids where fitting), `dex.js` (optional short lore per new monster), `tests/catalog.test.js` (counts + distinctness already enforce; bump expected count).
- 40 distinct, characterful monsters: unique recipes (≥2 non-none parts, pairwise ≥2-field distinct — test-enforced), fitting elem/skill(incl pull)/attack/move/quirk, tuned stats. Names + silhouette variety.
- Update any hardcoded expected-count test to 64; ensure BootScene bakes them (dynamic — no change).
- [ ] TDD (distinctness/coverage/count) → author 40 → green; commit `feat(v6): +40 monster species (64 total), distinct recipes + kits`.

### Task 9: Nest overhaul — finer grid, ~60 decor, pets use furniture (C2)
**Files:** `decor.js` (GRID 12×8; +36 items → ~60; paintDecor new shapes), `nestscene.js` (finer-grid placement + render sizing; furniture-interaction idle AI), `tests/decor.test.js` (≥60 items, categories, grid math), `tests/nest.test.js` (NestAI furniture states).
- `Decor.GRID = {cols:12, rows:8}`; retune cell px + decor render size.
- +36 everyday items across categories (furniture: sofa/bed/table/chair/bookshelf/fridge/TV/lamp/rug/clock/plant/dresser/mirror; toys: ball/yarn/ringtoss/plushie/scratchpost/tunnel; floor/bg/special variety) — parametric paintDecor shapes.
- NestAI: extend states so pets seek+use furniture (sit/sleep/watch/eat) mapped by category, not just toys; keep it legible + tested (new states reachable, category-gated).
- [ ] TDD (catalog + AI) → implement → green; commit `feat(v6): finer nest grid + ~60 everyday decor + pets use furniture`.

### Task 10: Cuter main menu with the player's pets (D1)
**Files:** `ui.js` MenuScene.
- Replace the 3 static monster jellies with the player's OWN owned pets (state.pets) roaming/playing via the NestAI idle; cozy nest-yard backdrop (placed decor + warm blobs), gentle motion, inviting layout. Keep all nav buttons (with padded hit areas from T4).
- [ ] Implement → `node --check` → commit `feat(v6): cozy main menu with your own pets frolicking`.

### Task 11: Flashier gacha reveal (D2)
**Files:** `shop.js` playReveal + `effects.js`.
- Longer anticipation (charging glow + rising pitch), light rays from the egg, rarity-colored full-screen wash, denser particles; dramatically bigger legendary treatment (slow-mo, gold god-rays, extra confetti+shake, "LEGENDARY!" title). Multi-pull: per-cell pop + rarity beam.
- [ ] Implement → commit `feat(v6): dramatic gacha reveal ceremony (esp. legendary)`.

### Task 12: Premium rarity cards (D3)
**Files:** `frames.js` (richer per-tier frames: inner glow, holo shimmer, rarity gem, drop-shadow, card proportions), `dex.js` + `shop.js` (card layout: pedestal + name plate + element/skill chips), `tests/frames.test.js`.
- Keep the 4 tiers + tween-leak-safe destroy; upgrade visuals to cute+premium trading-card look consistent with retro-neon. Layout: centered subject on a pedestal, name plate, element+skill chips.
- Tests: recipe fields still present/distinct; legendary animated; new fields (glow/gem) covered.
- [ ] TDD → implement → green; commit `feat(v6): premium refined rarity cards (dex + gacha)`.

### Task 13: Ship v6.0.0
- Full suite; `node --check` all; versionCode +1 / versionName "6.0.0"; `npx cap sync`; AAB build + manifest verify (6.0.0 present); RELEASE_STATUS.md v6 row + §1/§2; commit `release: v6.0.0`; merge to main (controller); refresh web demo (controller).

## Self-Review Notes
- Spec coverage: A1-A2→T1, A3→T2, A4-A5→T3, A6-A7→T4, B1→T5, B2→T6, B3→T7, C1→T8, C2→T9, D1→T10, D2→T11, D3→T12, ship→T13.
- Load-bearing risk: T1-T3 all move the 200-stage sim — each retunes + documents; T2 is the hardest (steeper monster power vs playability). Order T1→T2→T3 so the sim is retuned incrementally.
- Biggest content: T8 (40 monsters) + T9 (nest). Both are data+parametric, test-gated for distinctness.
