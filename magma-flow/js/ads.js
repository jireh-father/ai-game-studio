// ads.js — POC placeholder stubs
const Ads = {
  showInterstitial: (onComplete) => { if (onComplete) onComplete(); },
  showRewarded: (onReward, onSkip) => { if (onSkip) onSkip(); },
  showBanner: () => {},
};
